type: pages
label: Publications
parent: kirby.page('publications')
layout: cards
template: publication
image:
  query: page.images.template("cover").first
  ratio: 3/2
  cover: true
info: "{{ page.year }}"